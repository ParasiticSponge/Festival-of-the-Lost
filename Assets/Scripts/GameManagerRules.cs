using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManagerRules : MonoBehaviour
{
    //TODO: can slowly convert all instances of GetObjectOfType, store them here, then access them anywhere in scripts
    protected static 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
